async function main() {
  /* if (typeof window !== "undefined") {
    document.addEventListener("DOMContentLoaded", function () {
      var grLoader = grMobileLoader({
        onlineHash: "ONLINE_HASH", // Credentials for external API login.
      });
    });
  } */
}

main();
